services:
  orange-apple-assistant-web:
    build: .
    container_name: orange-apple-assistant-web
    env_file:
      - path: .env
        required: false
    environment:
      PORT: "8080"
    ports:
      - "8080:8080"
    restart: unless-stopped
